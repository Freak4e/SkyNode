# Client Shared

Planned home for frontend code that is not tied to one feature.

Examples:

- reusable layout components
- design-system primitives
- formatting helpers
- client-side validation helpers
- shared hooks
