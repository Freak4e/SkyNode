# Tests

Planned test structure for the bachelor project.

- `unit`: pure functions and small modules.
- `integration`: API routes and service flows with mocked providers.
- `fixtures`: deterministic sample API/HTML/LLM responses.

The testing approach is documented in `docs/testing-strategy.md`.
