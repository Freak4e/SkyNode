{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...path].ts": {
      "includeFiles": "dist/server/**",
      "regions": ["fra1"],
      "maxDuration": 60
    },
    "api/trips.ts": {
      "includeFiles": "dist/server/**",
      "regions": ["fra1"],
      "maxDuration": 60
    },
    "api/trips/[...path].ts": {
      "includeFiles": "dist/server/**",
      "regions": ["fra1"],
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
